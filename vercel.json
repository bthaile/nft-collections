{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd web && pnpm install && pnpm run build",
  "outputDirectory": "web/dist",
  "framework": "vite"
} 