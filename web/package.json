{
  "name": "nft-minter-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "mkdir -p contracts && cp ../artifacts/contracts/nft-contract.sol/MyNFT.json ./contracts/MyNFT.json",
    "build": "npm run prebuild && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0",
    "@types/node": "^20.0.0"
  }
} 